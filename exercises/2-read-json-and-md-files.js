const fs = require('fs');
const glob = require('glob');

// Read all 'json' and 'md' files in 'content' directory. So not the 'txt files'. Print the contents of the file.

// Use glob for pattern matching
// Use fs for reading the files

// Run this script with 'node exercises/2-read-json-and-md-files.js'